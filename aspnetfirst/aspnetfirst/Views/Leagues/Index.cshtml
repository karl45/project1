@model IEnumerable<aspnetfirst.Models.League>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<table class="table text-light">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.LeaugueName)
            </th>
            <th>

            </th>
            <th>

            </th>
            <th>

            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LeaugueName)
            </td>
            <td>
            <td>
                <a class="btn btn-outline-light" asp-action="Edit" asp-route-id="@item.LeagueId">Edit</a>
            </td>
            <td>
                <a class="btn btn-outline-light" asp-action="Details" asp-route-id="@item.LeagueId">Details</a>
            </td>
            <td>
                <a class="btn btn-outline-light" asp-action="Delete" asp-route-id="@item.LeagueId">Delete</a>
            </td>
            </td>
        </tr>
}
    </tbody>
</table>
<a class="btn btn-outline-light" asp-action="Create">Create New</a>